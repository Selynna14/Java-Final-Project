//Make array WeaponList. When you encounter a Weapon


public class CombatNotNeeded// can be deleted
{// can be deleted

	Public Combat(tileNum)
	{
		boolean combat = true; // creates flag variable
		while (combat = true)
		{
			int Damge = 0; 
			if (tileNum = 20) //need colitions
			{
				Monsters Zombie = new Zombie();//make a house!
				MonsterHealth = Zombie.getMonsterHealth;
				while (MonsterHealth > 0) // loops while the monster is alive
				{
					input = (String) JOptionPane.showInputDialog(null,"How would you like to attack?", "", JOptionPane.QUESTION_MESSAGE, null, WeponList, WeponList[1]);
					switch(input) 
					{
						case 'Hand': // code block 
							Damage = random(0,5); // does a random amount of damage from 0 to 4
							break;
						case 'Axe': // code block 
							Damage = random(5,10); 
							break;
						case 'Sword': // code block 
							Damage = random(8,12);
							break;
						case 'Poison Sword': // code block 
							Damage = random(10,14);
							break;
						case 'Poison Axe': // code block 
							Damage = random(7,12);
							break;
						default: // code block 
					} 
				MonsterHealth = MonsterHealth - Damage; // subtracts damage from monsters health

				Damage = Zombie.getMonsterDamage;
				Player health = Player health - Damage; //need player health
				}
			}


			if (tileNum = 21) //need colitions
			{
				Monsters Skeleton = new Skeletons();//make a house!
				MonsterHealth = Skeleton.getMonsterHealth;
				while (MonsterHealth > 0) // loops while the monster is alive
				{
				
					input = (String) JOptionPane.showInputDialog(null,"How would you like to attack?", "", JOptionPane.QUESTION_MESSAGE, null, WeponList, WeponList[1]);
					switch(input) 
					{
						case 'Hand': // code block 
							Damage = random(0,5); // does a random amount of damage from 0 to 4
							break;
						case 'Axe': // code block 
							Damage = random(5,10); 
							break;
						case 'Sword': // code block 
							Damage = random(8,12);
							break;
						case 'Poison Sword': // code block 
							Damage = random(10,14);
							break;
						case 'Poison Axe': // code block 
							Damage = random(7,12);
							break;
						default: // code block 
					} 
				MonsterHealth = MonsterHealth - Damage; // subtracts damage from monsters health

				Damage = Skeleton.getMonsterDamage;
				Player health = Player health - Damage; //need player health
				}
			}
			if (tileNum = 22) //need colitions
			{
				Monsters Witch = new Witch();//make a house!
				MonsterHealth = Witch.getMonsterHealth;
				while (MonsterHealth > 0) // loops while the monster is alive
				{
				
					input = (String) JOptionPane.showInputDialog(null,"How would you like to attack?", "", JOptionPane.QUESTION_MESSAGE, null, WeponList, WeponList[1]);
					switch(input) 
					{
						case 'Hand': // code block 
							Damage = random(0,5); // does a random amount of damage from 0 to 4
							break;
						case 'Axe': // code block 
							Damage = random(5,10); 
							break;
						case 'Sword': // code block 
							Damage = random(8,12);
							break;
						case 'Poison Sword': // code block 
							Damage = random(10,14);
							break;
						case 'Poison Axe': // code block 
							Damage = random(7,12);
							break;
						default: // code block 
					} 
				MonsterHealth = MonsterHealth - Damage; // subtracts damage from monsters health

				Damage = Witch.getMonsterDamage;
				Player health = Player health - Damage; //need player health
				}

			}

			if (tileNum = 23) //need colitions
			{
				Monsters Pixies = new Pixies();//make a house!
				MonsterHealth = Pixies.getMonsterHealth;
				while (MonsterHealth > 0) // loops while the monster is alive
				{
				
					input = (String) JOptionPane.showInputDialog(null,"How would you like to attack?", "", JOptionPane.QUESTION_MESSAGE, null, WeponList, WeponList[1]);
					switch(input) 
					{
						case 'Hand': // code block 
							Damage = random(0,5); // does a random amount of damage from 0 to 4
							break;
						case 'Axe': // code block 
							Damage = random(5,10); 
							break;
						case 'Sword': // code block 
							Damage = random(8,12);
							break;
						case 'Poison Sword': // code block 
							Damage = random(10,14);
							break;
						case 'Poison Axe': // code block 
							Damage = random(7,12);
							break;
						default: // code block 
					} 
				MonsterHealth = MonsterHealth - Damage; // subtracts damage from monsters health

				Damage = Pixies.getMonsterDamage;
				Player health = Player health - Damage; //need player health
				}
			}

			if (tileNum = 24) //need colitions
			{
				Monsters SoulOrb = new SoulOrb();//make a house!
				MonsterHealth = SoulOrb.getMonsterHealth;
				while (MonsterHealth > 0) // loops while the monster is alive
				{
				
					input = (String) JOptionPane.showInputDialog(null,"How would you like to attack?", "", JOptionPane.QUESTION_MESSAGE, null, WeponList, WeponList[1]);
					switch(input) 
					{
						case 'Hand': // code block 
							Damage = random(0,5); // does a random amount of damage from 0 to 4
							break;
						case 'Axe': // code block 
							Damage = random(5,10); 
							break;
						case 'Sword': // code block 
							Damage = random(8,12);
							break;
						case 'Poison Sword': // code block 
							Damage = random(10,14);
							break;
						case 'Poison Axe': // code block 
							Damage = random(7,12);
							break;
						default: // code block 
					} 
				MonsterHealth = MonsterHealth - Damage; // subtracts damage from monsters health

				Damage = SoulOrb.getMonsterDamage;
				Player health = Player health - Damage; //need player health
				}
			}

		combat = false;
		}
	}
}// can be deleted
